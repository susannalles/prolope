source "https://rubygems.org"
ruby RUBY_VERSION

# Hello! This is where you manage which Jekyll version is used to run.
# When you want to use a different version, change it below, save the
# file and run `bundle install`. Run Jekyll with `bundle exec`, like so:
#
#     bundle exec jekyll serve
#
# This will help ensure the proper Jekyll version is running.
# Happy Jekylling!

# La línea siguiente incluye los paquetes necesarios para el uso de Jekyll, y la versión:
gem "jekyll", "3.3.0"

# Este es el tema por defecto del sitio Jekyll. Para localizar los ficheros de este tema,
# debe ejecutarse 'bundle show minima'. Para modificar el tema, deben copiarse las carpetas
# y los ficheros y pegarse en nuestra carpeta del sitio Jekyll. 

gem "minima", "~> 2.0"

# Si el sitio se albergará en GitHub Pages, debe añadirse la gema correspondiente:
# To upgrade, run `bundle update github-pages`.

gem "github-pages", group: :jekyll_plugins

# If you have any plugins, put them here!
group :jekyll_plugins do
   gem "jekyll-feed", "~> 0.6"
end
